/* tslint:disable:no-unused-variable */
import { LocalTimePipe } from './local-time.pipe';

describe('Pipe: LoaclTimee', () => {
  it('create an instance', () => {
    let pipe = new LocalTimePipe();
    expect(pipe).toBeTruthy();
  });
});
